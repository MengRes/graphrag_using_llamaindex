# Option 1: create openai_key.txt in the project root (recommended, gitignored)
# Option 2: copy this file to .env and set the key
OPENAI_API_KEY=sk-your-key-here

# Optional: custom API base (OpenAI-compatible proxy)
# OPENAI_API_BASE=https://api.openai.com/v1
